{
  "name": "json_server_todos",
  "version": "1.0.0",
  "description": "using this json server as database for todo application for crud",
  "main": "index.js",
  "scripts": {
    "start" : "json-server --watch db_todos.json true --port 9000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
